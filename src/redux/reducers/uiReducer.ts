import { UiInterface } from '../types/uiTypes'
const initialState = {}

export default (state = initialState, { type, payload }: UiInterface) => {
  switch (type) {
    default:
      return state
  }
}